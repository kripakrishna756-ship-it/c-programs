CODE


#include <stdio.h>
int main() {
    int x = 5, y = 3;
    printf("AND = %d\n", x & y);
    printf("OR = %d\n", x | y);
    printf("Left Shift = %d", x << 1);
    return 0;
}


OUTPUT

AND = 1
OR = 7
Left Shift = 10
